#pragma once

#include <ranges>
#include <algorithm>
#include <vector>
#include <array>
#include <memory>
#include <map>
#include <numeric>

namespace str = std::ranges;

using PointIndex = size_t;
using CellIndex = size_t;